[project]
name = "dma_prime_vis_toolkit"
version = "1.5.8"
description = "DMA-PRIME Feb 2025"
dependencies = [
    "waitress",
    "flask<2.4",
    "flask-bcrypt",

    "mysqlclient",

    "pandas",
    "openpyxl",
    "numpy",
    "geojson",
    "glob2",

    "requests",

    "google-api-python-client",
    "google-auth",
    "google-auth-oauthlib",
    "google-auth-httplib2",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"